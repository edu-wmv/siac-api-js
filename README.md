Initial project - SIAC

## Installation
 - Clone the repository
 - Install the dependencies
 - Modify the .env.template file with your credentials
 - Run the project with `npm start` | `yarn start` | `pnpm start`